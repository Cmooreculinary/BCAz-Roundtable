# SQLite database location
DB_NAME=roundtable_vo
SQLITE_PATH=data/roundtable_vo.sqlite3

# Local file upload storage
UPLOAD_ROOT=data/uploads
# Maximum accepted upload size in bytes (default: 50 MiB)
MAX_UPLOAD_BYTES=52428800
APP_NAME=Roundtable_VO

# Production values must point at durable storage supplied by the backend host:
# SQLITE_PATH=/path/to/persistent-data/roundtable_vo.sqlite3
# UPLOAD_ROOT=/path/to/persistent-data/uploads

# JWT signing secret — generate a strong random string
JWT_SECRET=change_me_to_a_long_random_string

# Optional first-run administrator seed. No default password is built into the app.
# Leave ADMIN_PASSWORD blank to disable automatic administrator creation.
ADMIN_EMAIL=admin@roundtable.app
ADMIN_PASSWORD=

# Allowed frontend origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://your-project.vercel.app

# Web Push (VAPID) — generate with: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print(v.private_key, v.public_key)"
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_EMAIL=you@example.com

# Twilio SMS (optional — leave blank to disable SMS bridge)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+1...

# Anthropic API key (for Smart Suggestions — optional, leave blank to disable)
ANTHROPIC_API_KEY=sk-ant-...

# Per-client-IP login/registration attempts per 60 seconds (single backend process).
AUTH_RATE_LIMIT=30
