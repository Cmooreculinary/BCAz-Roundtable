# Persistent FastAPI backend URL. Production must be HTTPS and support long-lived
# WebSockets plus durable database/upload storage. Set this in Vercel before deploy.
REACT_APP_BACKEND_URL=http://localhost:8001
